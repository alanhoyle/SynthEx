.First <- function(){
  message ("\nStarting R with Synthex at", date(),"\n")
  library(SynthEx)
  intersectBed.dir <- system ("which intersectBed",intern=TRUE)

}

.Last <- function () {

  message ("\nFinishing R with Synthex at", date(),"\n")
}
